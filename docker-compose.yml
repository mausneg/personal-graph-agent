services:
  database:
    build:
      context: .
      dockerfile: database/Dockerfile
    container_name: agent_db
    env_file:
      - .env
    ports:
      - "${POSTGRES_PORT}:5432"
    
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: